{
  "framework": "static",
  "build": {
    "command": "npm run build",
    "outputDirectory": "dist",
    "installCommand": "npm install"
  },
  "routes": [
    { "src": "/", "dest": "/index.html" }
  ]
}
